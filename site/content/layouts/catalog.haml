- catalog = Catalog.new(page_articles).send(current_page.data.include)

- wrap_layout :page do
  %article.catalog
    - catalog.each do |book|
      = partial 'layouts/description', locals: {book: book, cover_link: book.cover_link}

